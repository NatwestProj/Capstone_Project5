apiVersion: apps/v1
kind: Deployment
metadata:
  name: capstone-sample-app
  labels:
    app: capstone-sample-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: capstone-sample-app
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: capstone-sample-app
    spec:
      containers:
        - image: sunnynatwest/mock-api:4cda6f890d279fafa9d571a9a68f18e99e50923c
          name: capstone-sample-app
          ports:
            - containerPort: 5000
              name: http-server
      imagePullSecrets:
        - name: ecr-secret
